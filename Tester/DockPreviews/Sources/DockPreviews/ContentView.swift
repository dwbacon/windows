
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Dock Previews")
                .font(.largeTitle)
            SettingsView()
        }
        .padding()
    }
}
